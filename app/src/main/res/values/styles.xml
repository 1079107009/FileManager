<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="toolbar_theme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textColorSecondary">@color/text_white</item>
        <item name="android:textColorPrimary">@color/text_white</item>
        <!--设置弹出位置的主题-->
        <item name="actionOverflowMenuStyle">@style/OverflowMenuTheme</item>
    </style>

    <style name="OverflowMenuTheme" parent="Widget.AppCompat.Light.PopupMenu.Overflow">
        <!--这个属性设置为false，就能使得popup在Toolbar的下面显示-->
        <item name="overlapAnchor">false</item>
    </style>

    <style name="appCompatLight" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
        <item name="popup">@style/ThemeOverlay.AppCompat.Light</item>
        <item name="toolbar_theme">@style/toolbar_theme</item>
    </style>

    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <!--<item name="spinBars">true</item>-->
        <item name="color">@android:color/white</item>
    </style>
</resources>
